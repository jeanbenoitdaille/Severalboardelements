let prenoms = ["Pierre", "Paul", "Marie"];
     
let premier = prenoms[0];
let dernier = prenoms[prenoms.length - 1];
let deux_premiers = prenoms.slice(0, 2);
let deux_derniers = prenoms.slice(1, 3);